# Setup Project
* Create project
* Run project

### Create project
```sh
spring init \
    --java-version=1.8 \
    --dependencies=web \
    server &&
```

### Run project
```sh
cd server &&
idea .
```
