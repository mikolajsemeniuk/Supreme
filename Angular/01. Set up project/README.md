# Set up project
* Install CLI (❗if You do not have it)
* Create Project
* Run project

### Install CLI
in `terminal`
```sh
npm install -g @angular/cli
```
### Create Project
in `terminal`
```sh
ng new UI
```
### Run project
in `terminal`
```sh
cd UI
ng serve
```
